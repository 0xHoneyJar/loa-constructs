---
agent: skill
agent_path: .claude/skills/adding-skills/SKILL.md
context_files:
  - construct.yaml
---

# /skill-add

Add a new skill to the current construct. Two-phase scaffolding: shell first (create), capabilities later (skill-add).

Usage: `/skill-add <name>`
