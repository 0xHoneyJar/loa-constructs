name: browsing-constructs
version: "1.0.0"
description: |
  Browse and install packs from the Loa Constructs Registry with multi-select UI.
  Enables composable skill installation per-repo.

model: sonnet
color: purple

triggers:
  - pattern: "/constructs"
    description: "Browse and install packs"
  - pattern: "/constructs browse"
    description: "Browse available packs"
  - pattern: "/constructs install"
    description: "Install specific pack"
  - pattern: "/constructs register"
    description: "Register a new construct from a git repo"
  - pattern: "/constructs sync"
    description: "Sync a git-sourced construct from registry"
  - pattern: "/constructs status"
    description: "Show sync status for installed constructs"

dependencies: []

inputs:
  - name: action
    type: string
    description: "Action to perform: browse, install, list, update, uninstall, register, sync, status"
    required: false
    default: "browse"
  - name: pack
    type: string
    description: "Pack slug for install/uninstall actions"
    required: false

outputs:
  - name: installed_packs
    type: array
    description: "List of installed pack slugs"
  - name: result
    type: object
    description: "Installation result with success/error info"

zones:
  system: read
  state: write
  app: none
