{
  "buildCommand": "cd ../../packages/shared && bun run build && cd ../../apps/explorer && bun run build",
  "installCommand": "cd ../.. && bun install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/reconcile",
      "schedule": "*/15 * * * *"
    }
  ]
}
