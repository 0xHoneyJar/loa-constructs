# Publishing construct-creator

Pack is cycle-006 L-meta-pack complete (reframed 2026-04-23 late from "exemplar" to "apprenticeship" per [[accelerated-learning-surface]] doctrine): validates clean, CONSTRUCT-README.md autogenerated, CURATOR persona ratified via [[naming-is-diagnostic]] pass, two-register design (creating + wayfinding) in place. **Upstream publish is the F33 closure step and requires operator sign-off on the reframe before the external repo lands under 0xHoneyJar.**

## One-command publish (after operator review)

```bash
# From inside packs-in-flight/construct-creator/
cd packs-in-flight/construct-creator

# Initialize + first commit
git init -b main
git add .
git commit -m "feat: construct-creator v0.1.0 — CURATOR apprenticeship + wayfinding pack per cycle-006 L-meta-pack"

# Create upstream repo + push in one shot (requires gh auth + 0xHoneyJar org access)
gh repo create 0xHoneyJar/construct-creator \
  --public \
  --source . \
  --description "Apprenticeship + wayfinding — CURATOR teaches construct authorship (creating register) and finds the right existing construct (wayfinding register)" \
  --homepage "https://constructs.network/constructs/construct-creator" \
  --push
```

## What operator should review first

1. **CURATOR's two-register voice** — `identity/CURATOR.md`. Museum-curator metaphor (curate collection + guide new works) should feel natural across both registers. If it strains, rename one register or split the persona.
2. **Creating-stage machine** — `skills/creating-constructs/SKILL.md`. 11 stages: intent → naming → scaffold → streams → composition → skills → persona → validate → butterfreezone → reference composition → publish. Are there missing stages? Redundant stages? Refusal rules too strict / too loose?
3. **Taste stack composition** — `construct.yaml` declares `compose_with: [hivemind, artisan, k-hole, the-arcade]`. Same four lenses inform both registers. Operator may want a fifth (e.g. `observer` for user-truth) or drop one if usage shows it's redundant.
4. **Reference composition** — `compositions/explore-ecosystem.yaml`. Two-stage CURATOR → ALEXANDER. Demonstrates wayfinding. Cycle-007 target: author a parallel `author-a-construct.yaml` demonstrating the creating register as a composition.
5. **Relationship to construct-base** — does README clearly articulate "template + teacher = full authorship arc"? If a new author reads both READMEs, do they know when to reach for which?

Operator refinements feed back into the pack BEFORE first upstream commit. After publish, amendments ship as v0.1.1, v0.2.0, etc.

## What ships as-is (no operator review needed)

- **construct.yaml schema v3 conformance** — passes `construct-validate.sh` with zero findings
- **CONSTRUCT-README.md autogeneration** — produced by `butterfreezone-construct-gen.sh` dogfooding cycle-005 L6
- **CLAUDE.md grimoires declaration** — doctrine v5 §17.4 compliance
- **File layout** — identity/, skills/ (2 skills), commands/ (2 commands), compositions/ matches ecosystem norm
- **Two-register pattern** — documented in CLAUDE.md + CURATOR.md + README.md; operator picks register via skill invocation

## After publish

Register the pack slug in the constructs.network explorer:
- Run global sync (`pnpm seed:auto` from constructs repo) OR
- Let the next sync cycle pick it up via namespace scan

Post-publish validation:
```bash
gh repo view 0xHoneyJar/construct-creator
constructs-browse construct-creator        # verify listing
constructs install construct-creator       # verify install
/explore-network "test query"              # verify wayfinding register fires
/create-construct                          # verify creating register fires
```

## Roll back

If the pack lands and operator wants to iterate:
```bash
# Pack stays in registry; bump version + push amendments
vim construct.yaml  # bump version
git commit -am "feat: refine CURATOR voice"
git push
```

To fully remove (destructive):
```bash
gh repo delete 0xHoneyJar/construct-creator  # requires confirmation
```

## Migration note (if the old slug was already published)

If `construct-network-tools` was published upstream before this reframe: that repo can stay as an archived/deprecated shell with a `README.md` redirect to `construct-creator`. Global sync will pick up the new slug automatically. Operator-driven call.

As of 2026-04-23 late, the old slug was NOT published — the reframe happened in `packs-in-flight/` before the first `gh repo create`. Clean slate for `construct-creator`.

---

*Cycle-006 L-meta-pack close. The one-command publish is ready; operator gates it. Reframe from "exemplar" to "apprenticeship" honors the [[accelerated-learning-surface]] doctrine the operator ratified during cycle-006 authorship.*
