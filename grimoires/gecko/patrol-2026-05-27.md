# GECKO Patrol — 2026-05-27 (TEND cycle observation)

> operator-paced TEND. observation only — no closes, no labels, no canvases. findings feed into next session's tend-action phase. #246/#247/#248 (today's lab triple) out of scope.

## Counts

- alive: 14
- zombie: 5
- superseded: 6
- resolved-but-open: 6
- needs-operator: 6

## Patterns the bazaar shows

three orbits sit in the wall right now. the **rfc-orbit** (#78/#79/#80/#103-108/#127/#128/#145) — feb 2026 thinking, no commits ever cite them, mostly notes. the **substrate-orbit** (#117/#125/#129/#181/#222/#227/#240) — real engineering load, most of it shipped silently and the issues never closed. the **post-eileen-orbit** (#189) — eileen explicitly froze hivemind work for ~1yr until dnft/freeside ships and operator said ok. then a small alive cluster around #240 (drizzle drift), #126 (drm/distribution), #224 (composition soak couldn't run). the wall is fuller of resolved-but-open and superseded than of alive — most observed drift is the close-bookkeeping kind, not the unfinished-work kind.

## Per-issue verdicts

#243 [2026-05-12] [15d] Proposal: schema_version: 4 · optional construct.yaml top-level fields for doctrine compounding
  ├─ verdict:   ALIVE
  ├─ evidence:  `.claude/schemas/network/construct.schema.json` still at schema_version 3 with `additionalProperties: false` at root — no v4 bump yet
  └─ next:      canvas-and-track

#242 [2026-05-11] [16d] cycle-craft-cluster BB v2 follow-ups (PR #241, 0 blockers, 1 MED + 5 LOWs)
  ├─ verdict:   ALIVE
  ├─ evidence:  PR #241 merged at commit `3212e6e2`; follow-ups (lane evidence asymmetry, `dispatch_timestamps` provenance, symlink hygiene) are real next-cycle items — none referenced in subsequent commits
  └─ next:      canvas-and-track

#240 [2026-05-11] [16d] schema-drift: prod packs table 11 columns behind Drizzle; no migration generated
  ├─ verdict:   ALIVE
  ├─ evidence:  `apps/api/drizzle/` latest is 0016 (2026-05-06); columns `short_description`/`logo_mark`/`logo_wordmark` exist in `apps/api/src/db/schema.ts` but no migration file generates them — journal still out of sync
  └─ next:      canvas-and-track

#227 [2026-05-09] [18d] Substrate consumer migration — top-12 strict pack manifests + audit-feel composition (post-v2.40.0)
  ├─ verdict:   ALIVE
  ├─ evidence:  `compositions/audit-feel.yaml` exists (1 composition, not migrated top-12); pack manifests at `.claude/schemas/network/pack-manifest.schema.json` use `version` field still — strict-tier migration is sparse
  └─ next:      canvas-and-track

#224 [2026-05-04] [23d] Drift soak check (week 1 post-governance) — PARTIAL: access scope blocked validate-strict.ts
  ├─ verdict:   NEEDS-OPERATOR
  ├─ evidence:  report self-flags as blocked by mcp scope; rerun would need a session with loa-compositions + loa-hivemind access — operator must decide whether to rerun or defer
  └─ next:      escalate

#222 [2026-04-28] [29d] [ARCH] bash compose-run + claude -p is the wrong runtime — replace with TeamCreate-native
  ├─ verdict:   ALIVE
  ├─ evidence:  `.claude/scripts/compose-run.sh` + `.claude/scripts/stage-executor-tmux.sh` both still present; no TeamCreate-native runner shipped; operator-declared deferred ("will return later, not blocking now")
  └─ next:      leave-be

#189 [2026-04-21] [36d] Construct-Network Redesign — generalizing spiraling-v2 principles (Eileen #606) to the construct layer
  ├─ verdict:   ZOMBIE
  ├─ evidence:  eileen's 2026-04-21 comments explicitly freeze hivemind work for ~1yr until dnft/freeside ships; operator replied "ok" on 2026-04-22; no commits since
  └─ next:      close

#184 [2026-03-24] [64d] Spawned agents don't inherit construct context — SKILL.md / rules invisible to teammates
  ├─ verdict:   ALIVE
  ├─ evidence:  no `.claude/agents/` directory exists for construct-context inheritance; only k-hole adapter agents-test fixture references found; problem is upstream-runtime and not patched in this repo
  └─ next:      escalate

#183 [2026-03-24] [64d] feat: /evolve-os — Operator OS discovery and evolution skill
  ├─ verdict:   ALIVE
  ├─ evidence:  `.claude/skills/evolve/` exists but is a contract-upgrade skill (different concern); no `evolve-os` slug anywhere; the 5-phase mode-discovery skill described in the rfc never shipped
  └─ next:      canvas-and-track

#182 [2026-03-22] [66d] Signal pipeline consistency: document contract, fix mobile gap, clean phantom deps
  ├─ verdict:   NEEDS-OPERATOR
  ├─ evidence:  cubquests + honey-interface live in separate repos; mobile-flex gap and signals.md doc are cross-repo work — can't verify from loa-constructs alone
  └─ next:      escalate

#181 [2026-03-19] [69d] RFC: Constructs Network — Install, Discovery & Schema Foundation
  ├─ verdict:   SUPERSEDED
  ├─ evidence:  the network-migration cycle (PR #225 `1f7d9e67`, 2026-05-06) + bounded-context cycle (PR #226) + cycle-rooms-observatory (PR #239) shipped most concrete items; operator's own addendum thread points to #182/#183/#184 as the dependency chain
  └─ next:      close

#145 [2026-03-04] [84d] [RFC] Construct Piping — Knowledge Flow from Signals to Composition
  ├─ verdict:   SUPERSEDED
  ├─ evidence:  `.claude/schemas/runtime/construct-handoff-v0.schema.json` + `construct-invocation-v0.schema.json` + `composition.schema.json` shipped via PR #226 / cycle-bounded-context; pair-relay reference composition (PR #241) is the working piping
  └─ next:      close

#129 [2026-02-19] [97d] Construct-owned workflow depth: manifests should declare their own pipeline gates
  ├─ verdict:   RESOLVED-BUT-OPEN
  ├─ evidence:  `.claude/loa/CLAUDE.loa.md` NEVER rules explicitly yield "OR when a construct with declared `workflow.gates` owns the current workflow"; schema has `workflow: {depth, gates}` block; the yield-pattern is live doctrine
  └─ next:      close

#128 [2026-02-18] [98d] [RFC] The Verification Gradient — Construct Composability, Echelon Integration & Behavioral Conviction
  ├─ verdict:   NEEDS-OPERATOR
  ├─ evidence:  rfc cross-filed to loa#369; operator-authored vision piece; no concrete work in loa-constructs; depends on echelon (external) status — operator weighs whether the frame is still active or moot
  └─ next:      escalate

#127 [2026-02-17] [99d] [Feedback] Constructs need golden-path-style workflow guidance — progressive leveling for packs - v1.39.0
  ├─ verdict:   SUPERSEDED
  ├─ evidence:  references closed #119 (RFC: construct golden path) which itself closed 2026-03-06; framework now ships golden-path skill (`.claude/skills/loa/`, `/loa` command in loa); progressive-leveling never built but the parent frame closed
  └─ next:      close

#126 [2026-02-17] [99d] [Feedback] Construct copy protection & distribution security model — design discussion
  ├─ verdict:   ALIVE
  ├─ evidence:  no DRM/copy-protection scheme in `.claude/scripts/constructs-install.sh` or anywhere in apps/api — the question (how to prevent simple copy of constructs) remains genuinely unanswered
  └─ next:      label-and-keep

#125 [2026-02-17] [99d] feat: authenticated git clone for private construct repos
  ├─ verdict:   RESOLVED-BUT-OPEN
  ├─ evidence:  `apps/api/src/services/git-sync.ts` uses `process.env.GITHUB_SYNC_TOKEN || process.env.GITHUB_TOKEN`; auth path wired both server-side and client-side
  └─ next:      close

#122 [2026-02-16] [100d] [Construct] Envio Indexer — CLI workflows, plan/execution integration
  ├─ verdict:   ALIVE
  ├─ evidence:  `~/Documents/GitHub/thj-envio` exists but no `construct-envio*` repo; no envio entries in `.agents/skills/` or any pack manifest — the construct proposal hasn't been materialized
  └─ next:      label-and-keep

#118 [2026-02-13] [103d] Distinguish construct topology from methodology — add reference ingestion pipeline
  ├─ verdict:   ALIVE
  ├─ evidence:  no `methodology/` or `references/` ingestion path in any pack schema; `construct.schema.json` has `domain` and `streams` but no methodology-vs-topology split; reference ingestion mentioned but unbuilt
  └─ next:      label-and-keep

#117 [2026-02-11] [105d] feat: Construct schema migration tooling for downstream consumers
  ├─ verdict:   RESOLVED-BUT-OPEN
  ├─ evidence:  `.claude/scripts/migrate-skill-names.sh`, `migrate-skill-roles.sh`, `migrate-grimoires.sh`, `migrate-state-layout.sh`, `schema-validator.sh` all present; topology validator at `scripts/validate-topology.sh` is CI-gated per MEMORY.md
  └─ next:      close

#116 [2026-02-11] [105d] [Constructs] Observer field report — friction, meta-learnings from 7-day deployment
  ├─ verdict:   ZOMBIE
  ├─ evidence:  field-report-as-issue from observer 1.4.0 on midi-interface; observer is now v3.0.0 (per MEMORY.md); specific frictions reported are upstream-of-substrate work that has since shipped or moved
  └─ next:      close

#110 [2026-02-07] [109d] [Feedback] Evidence fidelity loss in conversation ingestion — images detached from message context
  ├─ verdict:   NEEDS-OPERATOR
  ├─ evidence:  no comments since filing; observer construct lives in its own repo (`construct-observer` per MEMORY.md cycle-001); whether image-context fix shipped in observer needs cross-repo check
  └─ next:      escalate

#109 [2026-02-07] [109d] Event Bus Migration Guide — Learnings from midi-interface (Observer + Artisan)
  ├─ verdict:   RESOLVED-BUT-OPEN
  ├─ evidence:  `.claude/schemas/event-envelope.schema.json` is the cloudevents-shaped envelope; the migration pattern itself is now schema reality; doc-as-issue can be promoted to a runbook or just closed
  └─ next:      close

#108 [2026-02-06] [110d] Progressive Expertise — Onboarding Flow + Expertise Metrics
  ├─ verdict:   ZOMBIE
  ├─ evidence:  pairs with #79 (personalized onboarding) and #127 (golden-path leveling); none of the three have downstream activity; `.claude/skills/designing-progression/` exists but is a separate generative-game skill, not progressive-expertise
  └─ next:      close

#107 [2026-02-06] [110d] Template Framework Registry — Curated Agent-Mastered Tech Stacks
  ├─ verdict:   ZOMBIE
  ├─ evidence:  no commits cite #107; framework registry concept never built; `next-best-practices` skill exists as the seed it described but registry layer didn't follow
  └─ next:      close

#106 [2026-02-06] [110d] [RFC] Scribe Pack — Writing DX with Revision Tracking + Content Taste
  ├─ verdict:   ALIVE
  ├─ evidence:  no `construct-scribe` repo; no scribe slug in `.agents/skills/`; rfc was operator-authored vision; not built but the writing-DX gap is real and unfilled
  └─ next:      label-and-keep

#105 [2026-02-06] [110d] Token Efficiency Instrumentation — Track Tokens-to-Destination per Pack
  ├─ verdict:   NEEDS-OPERATOR
  ├─ evidence:  loa upstream now has MODELINV v1.3 envelope (per CLAUDE.loa.md "Multi-Model Activation" section) that captures per-invocation tokens — whether per-pack rollup happens or not depends on dashboard work the operator owns
  └─ next:      escalate

#104 [2026-02-06] [110d] [RFC] Unified Feedback Schema — Cross-Domain Back-Pressure Infrastructure
  ├─ verdict:   RESOLVED-BUT-OPEN
  ├─ evidence:  CloudEvents-shaped FeedbackEvent is the de facto pattern via `event-envelope.schema.json`; #109 documents the working migration on midi-interface; the unified schema shipped
  └─ next:      close

#103 [2026-02-06] [110d] [RFC] Domain-Specific DX — Adaptive Interfaces per Work Type
  ├─ verdict:   ALIVE
  ├─ evidence:  no domain-aware interface routing in `.claude/skills/*/index.yaml`; capabilities stanza exists per skill but doesn't surface domain-mapped interfaces; the gap is real
  └─ next:      label-and-keep

#93 [2026-02-05] [111d] feat: Add skill-level semantic search to registry API
  ├─ verdict:   ALIVE
  ├─ evidence:  `apps/api/src/services/skills.ts:201` uses `ilike` (substring) on `skills.name`/`description`; `searchKeywords`/`searchUseCases` arrays searched via array-unnest — keyword-level shipped, semantic/embedding-level not (`.claude/schemas/embedding-index.schema.json` exists but no pgvector wiring)
  └─ next:      canvas-and-track

#91 [2026-02-04] [113d] [Feature] Auto-trigger agent-browser during design iteration workflows
  ├─ verdict:   NEEDS-OPERATOR
  ├─ evidence:  `agent-browser` skill exists in the deferred-tool list (mentioned in system reminders); auto-trigger heuristic never built; question is whether design-iteration auto-routing is still wanted now that artisan + rosenzu have evolved
  └─ next:      escalate

#80 [2026-02-03] [114d] [RFC] Agent Feedback Loop Pattern
  ├─ verdict:   ZOMBIE
  ├─ evidence:  rfc proposes grimoire-based agent-logs ingestion at `grimoires/beacon/agent-logs/`; no such directory exists in the repo; beacon construct lives elsewhere; pattern never materialized
  └─ next:      close

#79 [2026-02-03] [114d] [RFC] Personalized Onboarding & What's New
  ├─ verdict:   SUPERSEDED
  ├─ evidence:  framework now ships `/loa` golden-path command (per loa CLAUDE.loa.md "5 commands for 90% of users") + `update-loa` skill + butterfreezone agent-readme; the onboarding question shifted
  └─ next:      close

#78 [2026-02-03] [114d] [RFC] Confidence Decay & Staleness Detection
  ├─ verdict:   SUPERSEDED
  ├─ evidence:  loa upstream ships `detecting-staleness` skill (in skills list); confidence/provenance frontmatter contract is now governed via straylight (operator's own kernel) — the rfc's intent is now framework-level
  └─ next:      close

#60 [2026-02-01] [116d] [Feedback] Explorer UX: Domain colors, multiselect install, graduation levels
  ├─ verdict:   SUPERSEDED
  ├─ evidence:  commit `2bff39b3` (2026-04-22) "retire apps/explorer — now @sprawl-world/constructs-network" — the explorer this feedback targets has been lifted out of this repo
  └─ next:      close

#16 [2026-01-22] [125d] feat: Construct Namespace Isolation & Multi-Construct Composition
  ├─ verdict:   RESOLVED-BUT-OPEN
  ├─ evidence:  grimoire-based isolation (`grimoires/<construct>/`) is operator's own clarification in comments; magic markers v1.1.0 covered ownership; bounded-context cycle (`feat(cycle-construct-bounded-context)` PR #226) shipped the substrate
  └─ next:      close

#13 [2026-01-20] [127d] Feature: Channel support for pack versioning (stable/develop/canary)
  ├─ verdict:   ZOMBIE
  ├─ evidence:  no `channel` column on `packs` table in `apps/api/src/db/schema.ts`; no `/channels` endpoint in routes; sigil-centric framing (the original motivator) has moved on with sigil's own packaging — feature never built and no recent demand
  └─ next:      close

## Notable patterns / cross-references

- **the explorer migration is the single biggest invalidator** — commit `2bff39b3` retired `apps/explorer/` to sprawl-world; this alone supersedes #60 directly and removes the surface that several RFCs (#79 onboarding, #103 domain-specific DX) implicitly targeted. worth a comment thread or just close-them-all sweep.
- **the substrate-orbit shipped without close-bookkeeping**: #109 (event bus) + #104 (unified feedback) + #117 (migration tooling) + #125 (auth clone) + #129 (workflow gates) + #145 (construct piping) + #181 (network rfc) — all RESOLVED-BUT-OPEN or SUPERSEDED. PR #225 + PR #226 + PR #239 + PR #241 collectively cleared this orbit; the issues just didn't get closed.
- **the rfc-orbit (#78/#79/#80/#103-108)** has no commit references and shares Feb 3-6 timestamps — a single brainstorming sprint that fed the substrate work but never got walked back. about half merged into shipped substrate, half remain as ALIVE vision.
- **#189 is the only operator-frozen issue**: eileen's comment + operator's "ok" reply explicitly defer it ~1yr. distinct from zombie — the freeze is intentional, not drift.
- **#240 is the one real engineering gap with a known fix**: the missed `drizzle-kit generate` from cycle-rooms-observatory is reproducible and unblocking-shaped — easiest to clear in next tend-action.
- **#222 (TeamCreate-native runtime)** is operator-declared deferred, not zombie — `compose-run.sh` + `stage-executor-tmux.sh` still present, the architectural decision was made to leave-be until later cycle.
- **two issues sit in cross-repo limbo** — #110 (image fidelity in observer ingestion, now lives in `construct-observer`) and #182 (signal pipeline for cubquests/honey-interface, lives in those repos). can't verify from loa-constructs alone — both are NEEDS-OPERATOR by definition.
