# Drift Report — loa-constructs

> Generated by /ride 2026-05-05. Three-way analysis: code · legacy docs · stated context.
> Methodology: every claim verified against source. Markers in [brackets].

## Summary

| Drift Category | Count | Severity |
|----------------|-------|----------|
| Aligned | 9 | Healthy |
| Stale | 4 | High |
| Hallucinated | 0 | Critical |
| Ghost (documented, not in code) | 1 | Critical |
| Shadow (in code, undocumented) | 3 | Medium |
| Missing (no documentation) | 2 | Medium |

**Drift score: 7.5 / 10** (high alignment overall; some accumulated stale docs and shadow modules)

## Critical items

### Ghost: `apps/explorer` referenced in MEMORY.md but lifted out

**Source claim** (`MEMORY.md`):
> "Monorepo (Turbo + bun) — `api` (Hono/Railway), `explorer` (Next.js/Vercel)"

**Reality** (`README.md:144` + filesystem):
> "Per cycle-007: `apps/explorer` (wallet/wagmi bleed) lifted to sprawl-world as `@sprawl-world/constructs-network`."

`apps/` only contains `api/` and `docs/`. The MEMORY.md context that drove this /ride is stale. **Severity: Critical** because it misroutes any agent attempting to find explorer code in this repo.

### Stale: "Auto-sync: pnpm seed:auto"

**Source claim** (`MEMORY.md`):
> "Auto-sync: `pnpm seed:auto`. Namespace IS the registry."

**Reality** (`package.json:7,22`):
> `"packageManager": "bun@1.3.11"` — and `seed:auto` runs via `tsx`, not pnpm. Invocation is `bun run seed:auto`.

The CLAUDE.md root rule is "ALWAYS use bun." The MEMORY.md still uses `pnpm` phrasing. **Severity: High** — language drift in operator memory; commands look superficially right but reference wrong tooling.

## High items

### Stale: `audits/architectural-overview-2026-02-09.md`

Pre-cycle-007 (Feb 9). Cannot describe current architecture since explorer was lifted, several admin/discovery/dynamic-auth routes were added (cycle-038, cycle-039, cycle-040), and registry CLI plugin matured. **Recommend**: archive or rewrite.

### Stale: `apps/docs/network/audit-2026-03-17.md`

7+ weeks old. Predates current substrate-construct manifest schema work (per current branch `feat/substrate-construct-manifest-schema`).

### Stale: Multiple PRD candidates in `grimoires/loa/`

`prd.md`, `prd-bridgebuilder-remediation.md`, `prd-constructs-network-migration-2026-05-05.md`, `prd-cycle-015.md`, `prd-cycle-043-short-descriptions.md`, `prd-cycle-a.md`, plus two `prd.md.cycle-*-bak` snapshots. No clear canonical PRD. /ride is producing fresh `prd.md` to replace.

## Medium items

### Shadow: `apps/api/src/services/discovery.ts`

Service exists (referenced in admin route discoverQuerySchema, line 1072). No discovery flow documented in `docs/` or `apps/docs/`. Likely tied to `scripts/discover-constructs.ts`. **Recommend**: surface in api-surface docs.

### Shadow: `apps/api/src/services/git-sync.ts` and webhook handler

Webhook intake exists; idempotency table exists (`githubWebhookDeliveries`). End-to-end flow not documented in `docs/`. Only oblique reference in `MEMORY.md`. **Recommend**: write a webhook ops runbook or extend `apps/docs/network/operator.md`.

### Shadow: `services/blacklist.ts`

Module + tests present. No documentation. Unknown what's blacklisted (users? IPs? namespaces?). **Recommend**: name in CONTRIBUTING-PACKS.md or NAMESPACING.md.

### Missing: Dynamic Labs auth flow doc

Code: `routes/dynamic-auth.ts` + `lib/verify-dynamic-jwt.ts` (cycle-039 per env.ts:71). No standalone doc. PRD candidate `grimoires/loa/context/dynamic-labs-auth-integration.md` exists as plan but not as ratified Ground Truth.

### Missing: API-key system doc

Code: `routes/keys.ts` + `apiKeys` table (schema.ts:269). Cycle-040 introduced this. No standalone doc. **Recommend**: add to `docs/MAINTAINER_GUIDE.md` or extend `apps/docs/network/operator.md`.

## Aligned (healthy)

| Claim | Source | Reality | Status |
|-------|--------|---------|--------|
| Hono API, Node ≥20, bun packageManager | README, CLAUDE.md, MEMORY.md | package.json + apps/api confirms | [GROUNDED] |
| 30 Drizzle tables | implied | 29 in schema.ts + 1 in schemas/ | [GROUNDED] |
| RS256 preferred / HS256 deprecated | env.ts:51-60 comments | code matches | [GROUNDED] |
| CORS allowlist `constructs.network` + Vercel previews | README + app.ts:62-71 | code matches | [GROUNDED] |
| Stripe + Resend + Sentry + Upstash + R2 + Postgres | env.ts + dependencies | all present | [GROUNDED] |
| 4-layer schema sync invariant | CLAUDE.md root | layers all exist (no auto-detector but layers present) | [GROUNDED] |
| Maintenance mode for migration cutover | sdd-infrastructure-migration.md (referenced in app.ts:53) | middleware/maintenance.ts present | [GROUNDED] |
| AGPL-3.0 license | README badge + LICENSE.md | LICENSE.md confirms | [GROUNDED] |
| 5-tier invocation contract | README.md:78 | runtime lives in `loa` repo, not here | [INFERRED] — not contradicted, just out of scope |

## Re-read recommendation

The MEMORY.md context that prompted /ride is broader than this repo (covers full THJ ecosystem). For loa-constructs scope:
- Trust: README.md, CLAUDE.md (root + framework), env.ts contracts, CHANGELOG.md
- Distrust until verified: `audits/`, `apps/docs/network/audit-2026-03-17.md`, MEMORY.md claims about explorer/Convex/etc.
- Treat as planning, not truth: `grimoires/loa/context/*.md` files

## Drift score breakdown

- Code-to-README alignment: 9/10 (README is unusually accurate)
- Code-to-CLAUDE.md alignment: 9/10 (root CLAUDE.md is current)
- Code-to-MEMORY.md alignment: 6/10 (stale `apps/explorer` claim, pnpm phrasing)
- Code-to-audits/ alignment: 4/10 (3-month-old snapshot)
- Code-to-grimoires/loa/context/ alignment: N/A (planning, not truth)

**Composite drift score: 7.5/10**
