# Drift Report — loa-constructs

> Generated by /ride 2026-07-12. Three-way analysis: legacy docs ↔ code reality ↔ operator context (MEMORY/claims).
> Verification evidence inline. Categories: Missing / Stale / Hallucinated / Ghost / Shadow / Aligned.

## Summary

| Category | Count | Share |
|----------|-------|-------|
| Aligned | 12 | 41% |
| Stale | 8 | 28% |
| Ghost | 4 | 14% |
| Shadow (undocumented code) | 5 | 17% |
| Hallucinated | 1 | 3% |
| **Claims verified** | **30** | |

**Drift score: 59% aligned-or-benign → moderate-high drift, concentrated in the repo's most visible surfaces (README, CHANGELOG, SECURITY.md).** Core architecture claims are healthy; release/version surfaces and the Quick-start toolchain are not.

## Critical Items

### D-1 (GHOST, Critical) — README Quick-start toolchain no longer in tree
README.md:31-40 instructs `constructs-install.sh`, `constructs-active` (3 modes), `constructs-list`. Today's `/update-loa --force-restore` (commits fa595dcb, c58aae51) removed 13+ repo-custom scripts from `.claude/scripts/` — `constructs-active.sh`, `constructs-list.sh`, `constructs-create.sh`, `constructs-diff.sh`, `constructs-health.sh`, `constructs-link.sh`, `constructs-publish.sh`, `constructs-register.sh`, `constructs-sweep.sh`, `constructs-audit-index.sh`, `feedback-v3-emit.sh`, `feedback-classifier.sh`, `feedback-redaction.sh`, `check-feedback-status.sh`.
**Evidence**: current `.claude/scripts/` has ~5-6 constructs-* scripts; `.claude.backup.1783885618/scripts/` has 15. Zero bats tests reference the removed names (blast radius on CI: none), but the documented operator UX is broken. **Recovery path exists**: restore from `.claude.backup.*` via `.claude/overrides/` or re-vendor.

### D-2 (GHOST, Critical) — Composition schema + runner absent
README.md:110 claims "This repo owns the schema (`.claude/schemas/composition.schema.json`) + the runner (`compose-run.sh`)". Neither exists: schema lived at `.claude/schemas/runtime/composition.schema.json` (now only in `.claude.backup.1783885618/schemas/runtime/`); `compose-run.sh` is absent tree-wide (superseded by the `/compose` Form C runtime per skill doctrine). README not updated for the supersession.

### D-3 (STALE, Critical) — Version surfaces diverge four ways
README badge v2.41.0 (README.md:4) · CHANGELOG head 2.41.0 (2026-05-09) · GitHub Release v2.43.0 (2026-05-09) · git tag v2.50.4 (2026-06-30). Plus README.md:11 pins framework v1.130.0 vs `.loa-version.json` 1.196.0, PROCESS.md anchors "Loa v1.1.0", SECURITY.md supports "0.1.x/0.2.x". The post-merge sync (Issue #821 E) is not holding.

### D-4 (GHOST) — Explorer dev command
README.md:186 `bun --filter explorer dev # Explorer on localhost:3001` — `apps/explorer` does not exist; the same README (:168) records its lift to sprawl-world per cycle-007. Internal self-contradiction.

### D-5 (STALE→DISPUTED) — "Rail-agnostic, NONE live here" vs Stripe service layer
README.md:160 N-rails invariant says no payment rails live here. Code reality: `apps/api/src/services/stripe.ts`, `stripe-connect.ts`, `payouts.ts`; Stripe checkout/portal routes (`routes/subscriptions.ts:81,155`); `STRIPE_*` env keys (env.ts:38-49); plus dead top-level `api/{checkout,webhook,subscription}` Vercel stubs. Either the invariant is aspirational (Stripe pending extraction to freeside) or the doctrine changed — `docs/architecture/payment-responsibility.md` should arbitrate. Marked `[DISPUTED: README N-rails vs services/stripe.ts]`.

## Stale Items (non-critical)

| # | Claim | Source | Reality |
|---|-------|--------|---------|
| D-6 | `pnpm install` / `pnpm dev` quick start | apps/api/README.md:10-18 | Repo rule: ALWAYS bun (CLAUDE.md); bun.lock is the lockfile |
| D-7 | "packs table has NO category column" | operator MEMORY.md | Column EXISTS: schema.ts:594 + idx :615 + migration `src/db/migrations/0011_packs_category.sql`; categories taxonomy table :1385 + `/v1/categories` routes |
| D-8 | "~30 Drizzle tables" | prior ride 2026-05-05 | 35 now (34 schema.ts + 1 public-keys.ts) — 5 added in 68 days (trajectoryEvents :1416, consumerCursors :1455, webhookNonces :1471, rateLimitBuckets :1489, lastKnownRegistry :1511) |
| D-9 | "Auto-sync: pnpm seed:auto" | operator MEMORY.md | `bun run seed:auto` (package.json:23) |
| D-10 | HS256 "legacy, deprecated" | env.ts:51 comment | Still REQUIRED in production (env.ts:115-127 refine) — deprecation not completed |

## Shadow (code exists, undocumented)

| # | Code | Evidence |
|---|------|----------|
| D-11 | THREE migration directories with colliding numbering | `apps/api/drizzle/` (17 canonical drizzle-kit, 0000-0016) + `apps/api/drizzle/migrations/` (4 dated hand-authored w/ rollback pairs) + `apps/api/src/db/migrations/` (8 hand-written; `0010-0016` collide with drizzle/ tags) — no doc explains the split; `drizzle/meta/` has only 3 snapshots vs 17 journal entries (snapshot/journal mismatch) |
| D-16 | Dockerfile (bun) vs nixpacks.toml (npm) build-system conflict | `apps/api/Dockerfile` = `oven/bun:1.2.22` + `bun run dist/index.js`; `apps/api/nixpacks.toml` = `npm i` + `node dist/index.js`. Two build paths, contradictory + nixpacks violates bun-only rule. Which Railway uses is unverified |
| D-12 | 5 new event/infra tables (trajectory events, consumer cursors, webhook nonces, rate-limit buckets, last-known registry) | schema.ts:1416-1511 — absent from README/docs |
| D-13 | Webhook-authenticated registry refresh (HMAC + nonce + rate-limit, no admin role) | app.ts:155-159, routes/admin-refresh.ts — operational surface documented only in code comments |
| D-14 | 89 untracked `.agents/skills/*` dirs | Appeared with v1.196 update; neither tracked nor ignored |

## Hallucinated

| # | Claim | Reality |
|---|-------|---------|
| D-15 | Composition reference path `grimoires/compositions/discovery/audit-feel.yaml` (README.md:105 comment) | File exists there AND at `compositions/audit-feel.yaml` — duplicate sources; README's inline YAML references `construct: observer` while ecosystem renamed observer→beehive/keeper (MEMORY 2026-03-15). Borderline hallucinated composition body. |

## Aligned (verified healthy)

- Monorepo workspaces `apps/*` + `packages/*` = apps/api, apps/docs, packages/shared, packages/loa-registry (package.json:6) ✔
- Stack: Hono (app.ts:1) + Drizzle/PostgreSQL (drizzle.config.ts dialect) + zod env validation ✔
- Infra claims: Supabase Postgres/Upstash Redis/R2/JWT RS256+OAuth (env.ts:16-66) ✔
- CONSTRUCTS_ORG default `0xHoneyJar` (env.ts:69) ✔
- Maintenance-mode write freeze (app.ts:55, env.ts:44) ✔ — still active accommodation
- Stripe dual-webhook migration var (env.ts:41) ✔
- Responsibility-boundary doc `docs/architecture/payment-responsibility.md` exists ✔
- CODEOWNERS default @janitooor ✔ · AGPL-3.0 consistent ✔ · 17-18 CI workflows ✔
- audit-feel canonical composition exists ✔ (modulo D-15 duplication)
- construct.yaml as SoT for routing index (commit 09095a89) ✔ consistent with registry code
- API surface: ~116 route handlers / 21 routers under /v1 (app.ts:100-169; hand-counted excluding tests) — matches "registry API" ownership claim ✔

## Recommended Actions (priority order)

1. **Restore or re-home the network toolchain** (D-1/D-2): decide `.claude/overrides/` vs `scripts/` vendoring; upstream issue for force-restore eating downstream customizations.
2. **Re-arm the version-sync pipeline** (D-3): badge, CHANGELOG backfill 2.42→2.50.4, releases, SECURITY.md versions.
3. **Arbitrate the N-rails claim** (D-5): either annotate README with the Stripe-transitional reality or excise the rails.
4. **Fix README Develop + apps/api README quick starts** (D-4/D-6): remove explorer line; pnpm→bun.
5. **Document or collapse the dual migration dirs** (D-11).
