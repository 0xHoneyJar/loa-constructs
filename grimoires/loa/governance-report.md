# Governance Report — loa-constructs

> Generated by /ride 2026-05-05.

## Summary

| Artifact | Present? | Notes |
|----------|----------|-------|
| `CHANGELOG.md` | Yes (35 KB) | Maintained; semver-aligned per git tags |
| `CONTRIBUTING.md` | Yes (16 KB) | Plus `docs/CONTRIBUTING-PACKS.md` for pack authors |
| `SECURITY.md` | Yes | Standard disclosure policy expected; verify current contact |
| `LICENSE.md` | Yes | AGPL-3.0 |
| `.github/CODEOWNERS` | Yes | Default owner @janitooor; `.claude/` and `.loa.config.yaml*` reserved to @janitooor |
| `.github/PULL_REQUEST_TEMPLATE.md` | Yes | PR template enforced |
| `.github/BRANCH_PROTECTION.md` | Yes | Branch protection notes |
| `.github/dependabot.yml` | Yes | Dependency update config |
| `.github/ISSUE_TEMPLATE/` | Yes | Issue templates configured |
| `.github/workflows/` | Yes | CI workflows present (count not enumerated in this pass) |
| Semver tags | Yes | Latest `v2.36.0`; consistent v-prefix; unbroken sequence visible from tag log |
| Pack-author install template | Yes | `.github/install-constructs-app.md` for GitHub App install |

## Strengths

1. **Codeowners discipline** — `.claude/` (System Zone) and `.loa.config.yaml*` are CODEOWNERS-locked to @janitooor, matching the framework's zone-system rule that `.claude/` must not be edited directly.
2. **Maintained CHANGELOG** — 35 KB and growing; release process appears active.
3. **Multiple contribution paths** — separate guide for pack authors (`docs/CONTRIBUTING-PACKS.md`) vs. core contributors (`CONTRIBUTING.md`).
4. **Branch protection documented** — separate doc, not just GitHub UI config.
5. **Dependabot enabled** — automated dependency PRs.

## Gaps

| Gap | Impact | Recommendation |
|-----|--------|----------------|
| **Release process not documented in `RELEASING.md`** | Medium — `git tag` discipline relies on tribal knowledge | Add a one-page release runbook tying `package.json` version, git tag, CHANGELOG entry, and any post-merge automation |
| **Version mismatch**: `package.json` says 1.5.0; latest git tag is v2.36.0 | Low (likely intentional — workspace version vs. network release version) | Document the relationship in CHANGELOG or release runbook |
| **Multiple PRD candidates** in `grimoires/loa/` | Medium — operator confusion about canonical truth | /ride will produce a single new `prd.md`; archive others to `grimoires/loa/archive/` |
| **No SECURITY contact verified** | Unknown | Confirm `SECURITY.md` has a current security@ contact and reasonable disclosure timeline |
| **No code-of-conduct file** | Low | Optional — open-source projects sometimes ship `CODE_OF_CONDUCT.md` separately |
| **No published threat model** | Medium — registry handles auth tokens, license keys, pack integrity | Consider a `docs/THREAT_MODEL.md` covering registry-specific risks (pack tampering, license forgery, namespace squatting) |
| **No `.github/SUPPORT.md`** | Low | Could direct users to GitHub Issues vs. Discord vs. email |

## License compatibility

AGPL-3.0 is strong copyleft. Distributing constructs that depend on this server may impose obligations on operators. Recommendation: confirm `LICENSE.md` is consistent with `package.json` license field (not checked in this pass — package.json doesn't include a `license` key, which is itself a small gap).

## Recommendations (in priority order)

1. **Decide canonical PRD** — archive obsolete `prd-*.md` files in `grimoires/loa/`; replace with the fresh `grimoires/loa/prd.md` produced by this ride
2. **Add `RELEASING.md`** — document version bump + tag + CHANGELOG flow
3. **Set `license` in root `package.json`** to `"AGPL-3.0"` for npm consumers
4. **Threat model doc** — registry has unique risks; document them
5. **Verify security contact in `SECURITY.md`** — confirm currency

## Score: 8 / 10

Strong governance overall. CODEOWNERS + branch protection + Dependabot + multi-track contribution guides put this repo above average. Gaps are documentation-shaped, not control-shaped.
